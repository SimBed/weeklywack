<%= form_with model: current_user.rel_user_workouts.build, local: false do |form| %>
  <div><%= hidden_field_tag :workout_id, workoutshow.id %></div>
  <%= form.button type: 'submit', class: "btn btn-heart faded" do %>
      <i class="far fa-heart" aria-hidden="true"></i>
  <% end %>
<% end %>
